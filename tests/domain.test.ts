import test from 'node:test';
import assert from 'node:assert/strict';
import {readFile} from 'node:fs/promises';
import {books,resolveReference,address,compare,expand,chapterNeighbor,chapterRange,passageUrl} from '../app/lib/domain/references.ts';
import {createLocalAdapter,getCorpus,matchText,releaseId} from '../app/lib/domain/corpus.ts';
const adapter=createLocalAdapter(async path=>JSON.parse(await readFile(new URL('../app/public'+path,import.meta.url),'utf8')));
test('27 independent book identifiers, OSIS and USFM mappings, 260 chapters',()=>{assert.equal(books.length,27);assert.equal(new Set(books.map(b=>b.code)).size,27);assert.equal(books.reduce((s,b)=>s+b.verses.length,0),260);assert.equal(books[3].osis,'John');assert.equal(books[3].usfm,'JHN');});
test('aliases, canonical addresses, ranges and numeric order',()=>{assert.deepEqual(resolveReference('Romans 3:23'),resolveReference('ROM.3.23'));assert.deepEqual(resolveReference('rom.3.23'),resolveReference('Rom 3:23'));assert.equal(expand(resolveReference('Romans 3:23–26')[0]).length,4);assert.equal(expand(resolveReference('John 7:53-8:11')[0]).length,12);assert.equal(resolveReference('Romans 3-4')[0].end,'ROM.4.25');assert.equal(resolveReference('Jude')[0].end,'JUD.1.25');assert.equal(resolveReference('Jude 5')[0].start,'JUD.1.5');assert.equal(resolveReference('2 John 1:13')[0].start,'2JN.1.13');assert.equal(resolveReference('3 John 14')[0].start,'3JN.1.14');assert(compare('ROM.2.1','ROM.10.1')<0);assert.equal(resolveReference('ROM.3.23;ROM.5.1').length,2);assert.match(passageUrl(resolveReference('Romans 3:23')),/read\/ROM\/3/);});
test('reject OT, unknown books, impossible verses and malformed/reversed ranges',()=>{for(const input of ['Genesis 1:1','Romans 0:1','Romans 3:0','Romans 17:1','Romans 3:99','Jude 26','John 8:11-7:53','John 3:16-','John 3:16;John 3:15','ROM.3.23.1','James 1:1-2-3'])assert.throws(()=>resolveReference(input),undefined,input);});
test('chapter navigation crosses books and respects NT boundaries',()=>{assert.equal(chapterNeighbor('MAT',1,-1),null);assert.equal(chapterNeighbor('REV',22,1),null);assert.deepEqual(chapterNeighbor('MAT',28,1),{book:'MRK',chapter:1});assert.deepEqual(chapterNeighbor('PHM',1,1),{book:'HEB',chapter:1});});
test('every registered chapter loads with release, source mappings and complete coverage',async()=>{for(const b of books)for(let c=1;c<=b.verses.length;c++){const ch=await adapter.getChapter(b.code,c);assert.equal(ch.releaseId,releaseId);assert.equal(ch.coverage.length,b.verses[c-1]);for(const s of ch.segments){assert.equal(address(s.anchors[0]).book.code,b.code);assert.match(s.sourceId,/\.usj#content\//);assert.ok(s.text.length);}}});
test('real absence differs from unavailable data and unsupported edition',async()=>{const r=await adapter.getPassage(resolveReference('Acts 8:37'));assert.equal(r.segments.length,0);assert.equal(r.coverage[0].textState,'absent');assert.equal(r.coverage[0].dataState,'available');assert.ok(r.coverage[0].publisherNoteIds.length);const n=await adapter.getPublisherNotes('ACT',8);assert(n.some(n=>n.body.includes('TR includes')));assert.throws(()=>getCorpus('ESV'),{code:'unsupported-edition'});const broken=createLocalAdapter(async()=>{throw Error('broken')});await assert.rejects(()=>broken.getPassage(resolveReference('Acts 8:37')),{code:'unavailable-data'});const incomplete=createLocalAdapter(async()=>({}));await assert.rejects(()=>incomplete.getChapter('ROM',3),{code:'unavailable-data'});});
test('cross-chapter and disjoint passage output is ordered',async()=>{const r=await adapter.getPassage(resolveReference('John 7:53-8:11'));assert.equal(r.segments.length,12);assert.equal(r.segments[0].id,'JHN.7.53');assert.equal(r.segments[11].id,'JHN.8.11');const d=await adapter.getPassage(resolveReference('Romans 3:23;Romans 5:1'));assert.deepEqual(d.segments.map(s=>s.id),['ROM.3.23','ROM.5.1']);});
test('Scripture search: whole words, phrases, book filter, pages, empty and note exclusion',async()=>{assert(matchText('Grace and truth','grace truth'));assert(!matchText('disgrace','grace'));const r=await adapter.searchText('grace');assert(r.total>20);assert.equal(r.hits.length,20);const p=await adapter.searchText('grace','',2);assert.equal(p.page,2);assert.notEqual(p.hits[0].anchor,r.hits[0].anchor);const rom=await adapter.searchText('grace','ROM');assert(rom.hits.every(h=>h.book==='ROM'));const phrase=await adapter.searchText('"fall short of the glory of God"');assert(phrase.hits.some(h=>h.anchor==='ROM.3.23'));assert.equal((await adapter.searchText('zzzznonexistent')).total,0);assert.equal((await adapter.searchText('BYZ')).total,0);assert.equal((await adapter.searchText('')).total,0);assert.equal((await adapter.searchText('""')).total,0);});
