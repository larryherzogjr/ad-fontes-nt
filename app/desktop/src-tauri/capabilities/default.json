{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Open cited HTTPS resources in the system browser. No filesystem, account, shell, or database access.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    { "identifier": "opener:allow-open-url", "allow": [{ "url": "https://*" }] }
  ]
}
